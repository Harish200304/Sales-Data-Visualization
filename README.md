# Sales-Data-Visualization
# The sales data is loaded in csv or excel format. 
# The loaded data is cleaned and preprocessed using python.
# After cleaning and preprocessing of data numerical, categorical and data columns are created. 
# After the creation of these columns summary statistics of the sales data is performed. 
# Visualization task is performing using the sales data.
# Using the codes for the tasks performed, a web application is developed to visualize various sales related data.
